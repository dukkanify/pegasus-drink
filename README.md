# Pegasus Drinks (scaffold)

This repo contains a Vite + React + TypeScript skeleton matching the requested folder structure. Replace the placeholder files in `src/` with your real content. To run:

```bash
npm install
npm run dev
```
